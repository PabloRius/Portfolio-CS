import { createTheme } from "@mui/material/styles";

const lightTheme = createTheme({
  palette: {
    primary: { main: "#007EA7" },
    secondary: {
      main: "#00A8E8",
    },
  },
});

export { lightTheme };
